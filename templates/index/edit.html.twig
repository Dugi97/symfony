{% extends 'base.html.twig' %}


{% block body %}
    <form method="post" action="{{ path('edit', {id:articles.id}) }}">
        <input type="text" name="title" value="{{ articles.title }}">
        <button type="submit" name="submit">Send</button>
    </form>

{% endblock %}